<% @links.each do |link| %>
  <%= link_to link.short_link, "/#{link.short_link}", target: "_blank" %> (<%= link.original_link %>) - <%= link_to link.link_hits.count.to_s + " hits", show_link_path(link.short_link) %> <br />
<% end %>